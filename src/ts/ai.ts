// interface Field {
//     squares: JSX.Element[] | null[]
//     marks: string[] | null[]
// }

// export default function makeStep(field: Field, isXSelected: boolean, { iconX, iconO }) {
//     const squares = field.squares.slice()
//     const marks = field.squares.slice()
//     const icon = isXSelected ? iconO : iconX
//     const oponent = isXSelected ? 'x' : 'o'


// }